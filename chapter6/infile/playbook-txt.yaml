- name: In File
  hosts: all
  tasks:
  - name: Insert multiline content
    blockinfile:
      path: /home/ec2-user/welcome.txt
      create: true
      block: |
        Hello Mohamed
        Hello World
  - name: "Read a file content"
    shell: |
      cat /home/ec2-user/welcome.txt
    register: file_content
  - name: "Print the file content to a console"
    debug:
      msg: "{{ file_content.stdout }}"
  - name: Place line
    lineinfile:
      line: Hello ec2-userandra
      path: /home/ec2-user/welcome.txt
      create: true
  - name: Remove line
    lineinfile:
      line: Hello World
      path: /home/ec2-user/welcome.txt
      state: absent
  - name: "Read a file content"
    shell: |
      cat /home/ec2-user/welcome.txt
    register: file_content
  - name: "Print the file content to a console"
    debug:
      msg: "{{ file_content.stdout }}"
